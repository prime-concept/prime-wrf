import UIKit

// TODO: Rename to `Palette` when doing so won’t result in name collision
public protocol PaletteProtocol {
    var tintColor: UIColor { get }
    var navigationBarTintColor: UIColor { get }
}
